#class TestCourierAuth:
   # @allure.title('Проверка успешной авторизации  курьера при заполнении обязательных полей')
    #def test_successful_authorization(self,create_courier):
       # response = create_courier
        #assert response.status_code == 201 and response.text == Response.RESPONSE_REGISTRATION_SUCCESSFUL
        #assert response.json()['id'] and isinstance(response.json()['id'], int)